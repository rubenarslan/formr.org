Dear %{user},

This is an automated message about your formr studies.

Your studies have used %{used} of compute time in %{month}, which has reached your monthly limit of %{limit}. Compute time is the processing time your runs spend executing — rendering pages, evaluating R/OpenCPU code, branches, and so on.

To keep within the limit, the following studies have been set to non-public and had their automatic actions (scheduled emails, reminders, and other daemon-driven processing) switched off, so they are neither accepting new activity nor spending compute for now:
%{runs}

No data has been deleted, and your study contents and settings are untouched. These studies will NOT be reopened automatically. Once you have reduced your usage or arranged a higher limit with the site administrators, you can make each study public again yourself in its run settings — and re-enable "cron" there too if it relies on scheduled emails or other automatic actions.

You can review your compute usage here:
%{compute_url}

Instance: %{instance}
Time: %{time}

Best in Binary,
formr robots

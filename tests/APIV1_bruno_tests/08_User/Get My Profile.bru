meta {
  name: Get My Profile
  type: http
  seq: 1
}

get {
  url: {{host}}/v1/user/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
  res.body: isNotEmpty
}

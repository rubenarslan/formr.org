meta {
  name: Delete Survey
  type: http
  seq: 7
}

delete {
  url: {{host}}/v1/surveys/friend_list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}

meta {
  name: Change Run Settings public
  type: http
  seq: 4
}

patch {
  url: {{host}}/v1/runs/apitest
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "public": 2
  }
}

assert {
  res.status: eq 200
}

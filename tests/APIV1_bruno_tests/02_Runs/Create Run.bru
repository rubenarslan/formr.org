meta {
  name: Create Run
  type: http
  seq: 1
}

post {
  url: {{host}}/v1/runs/apitest
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 201
  res.body.name: eq "apitest"
  res.body.link: contains "apitest/"
}

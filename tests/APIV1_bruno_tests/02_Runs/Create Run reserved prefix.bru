meta {
  name: Create Run reserved prefix
  type: http
  seq: 2
}

post {
  url: {{host}}/v1/runs/api-test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 400
}

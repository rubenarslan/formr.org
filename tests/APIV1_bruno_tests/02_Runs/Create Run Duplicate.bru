meta {
  name: Create Run Duplicate
  type: http
  seq: 3
}

post {
  url: {{host}}/v1/runs/apitest
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 409
  res.body.message: eq A run with the name 'apitest' already exists.
}

meta {
  name: Get Run Files
  type: http
  seq: 2
}

get {
  url: {{host}}/v1/runs/apitest/files
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
  res.body[0].name: contains "test_image.png"
}

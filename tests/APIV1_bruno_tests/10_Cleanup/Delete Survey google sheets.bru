meta {
  name: Delete Survey google sheets
  type: http
  seq: 4
}

delete {
  url: {{host}}/v1/surveys/api_test_survey_google
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}

meta {
  name: Delete Survey run import
  type: http
  seq: 5
}

delete {
  url: {{host}}/v1/surveys/friend_list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}

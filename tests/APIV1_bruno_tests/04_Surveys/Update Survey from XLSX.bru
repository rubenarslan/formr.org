meta {
  name: Update Survey from XLSX
  type: http
  seq: 2
}

post {
  url: {{host}}/v1/surveys/
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file(assets/survey.xlsx)
}

assert {
  res.status: eq 200
  res.body.name: eq survey
}
